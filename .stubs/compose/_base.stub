version: '3'

services:
  app:
    build:
      context: ./
      dockerfile: Dockerfile
    image: $DIR-app #name of the image
    depends_on:
      - service #replace or delete if needed
    volumes:
      - ../..:/workspaces:cached

$COMPOSESERVICES

volumes:
  $COMPOSEVOLUMES